// percentile-social.js
// NOTE: Removed 'export' statement. Functions are assigned to the global percentileFunctions object.

// Add social metric percentiles here
// Example:
// percentileFunctions.sociability_score_percentile = (score) => {
//   const benchmarks = [0, 30, 50, 70, 85, 95, 100];
//   return calculatePercentileFromBenchmarks(score, benchmarks);
// };
